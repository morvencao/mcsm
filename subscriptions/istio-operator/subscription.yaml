apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: istio-operator-subscription
  namespace: istio-operator-sub
  labels:
    app: istio-operator
    apps.open-cluster-management.io/reconcile-rate: medium
spec:
  name: istio-operator
  channel: istio-operator-sub/istio-charts-repo
  packageFilter:
    version: 1.11.3
  packageOverrides:
    - packageAlias: istio-operator
      packageName: istio-operator
  placement:
    placementRef:
      name: istio-operator-placement
      kind: PlacementRule
